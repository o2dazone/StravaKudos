{
  "name": "strava_kudos",
  "verson": "0.0.3",
  "scripts": {
    "minify": "minify ./index.js > ./chrome-extension/index.js",
    "bundle": "cd ./chrome-extension && zip -j StravaKudos.zip index.js icon.png manifest.json",
    "build": "npm run minify; npm run bundle"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "minify": "^7.0.2"
  }
}
